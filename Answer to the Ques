Ans:
n=int(input("Enter the max limit: "))
z=int(input("Enter the number whose multiple ypu want to skip: "))

for x in range(1,n):
     if(x%z==0):
        continue
     print(x)  # not in if loop
     x=x+1
